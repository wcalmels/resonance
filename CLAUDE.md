# Resonance — Claude Code instructions

See [editors/CLAUDE.md](editors/CLAUDE.md) for full instructions.

**TL;DR:** `pip install -e packages/core` then `python -m resonance --help`. Always prefer minimal context modes (`tests`, `endpoint`, `module`) over full files.
