# Resonance — agent instructions

See [editors/AGENTS.md](editors/AGENTS.md) for full instructions.

**TL;DR:** Use specialized bots with minimal context. Run `python -m resonance context <file> --compare` before generating. Bot prompts live in `packages/core/resonance/bots.py`.
